#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int a0;
    long a1;
    char a2;
    float a3;
    double a4;
    scanf("%d %ld %c %f %lf", &a0, &a1, &a2, &a3 , &a4);
    printf("%d\n%ld\n%c\n%f\n%lf", a0, a1, a2, a3 , a4);   
    return 0;
}
